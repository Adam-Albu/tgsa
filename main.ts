function setup () {
    scene.setTile(2, assets.image`lava`, false)
    scene.setTile(14, assets.image`grass`, false)
    scene.setTile(7, assets.image`spawn`, false)
    scene.setTile(15, assets.image`goal`, false)
}
